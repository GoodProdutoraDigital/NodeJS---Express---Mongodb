<div align="center"> 
    <div class="jumbotron">
    <h1 class="display-3 mt-5">Bem vindo ao BlogApp!</h1>
        <p class="lead">Exercicio desenvolvido em nodeJS e tecnologia de banco de dados noSQL mongoDB. </p>
    <hr class="my-3">
        <p>Para este mini blog utilizamos sistemas de Autenticação, Validação, CRUD, Encripitação, Sessão e etc.</p>
        <a href="/user/login" class="btn btn-lg btn-primary" onclick="">Login</a>
        <a href="/user/registro" class="btn btn-lg btn-outline-secondary" onclick="">Cadastrar</a>
    </div>
    <div class="mt-5">
        <h3>Ultimas postagens:</h3>
    </div>
</div>

{{#each postagens}}
    <div class="card mt-3">
        <div class="car-body">
            <h3 class="text-justify mx-3" align="right">{{titulo}}</h3>
            <p class="text-justify ms-2">{{conteudo}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-primary btn-sm ms-2">Continuar lendo</button></a>
            <hr>
            <p class="text-justify ms-2">Data: {{data}}</p>
        </div>
    </div>
{{else}}
    <h5>Nenhuma postagem..</h5>
{{/each}}

<div class="mt-3"></div>